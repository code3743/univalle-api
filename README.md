# Univalle -API
Este proyecto pretende crear una API de los sistemas requeridos por la comunidad estudiantil
de la Universidad del Valle

## Importante 🎈
Para instalar ejecuta el comando 
    ``` npm install ```
    ``` npx playwright install```

cambiar el **.env.ejemplo** por **.env**

### Requisitos
- Nodejs >= 14
